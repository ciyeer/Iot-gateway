name: IotEdgeGateway
env: development

paths:
  config_root: config
  data_dir: data
  log_file: logs/iotgw-dev.log

logging:
  level: info
  file_sink_enabled: true

network:
  http_api:
    host: 0.0.0.0
    port: 8080
    base_path: /api
  websocket:
    path: /ws

mqtt:
  enabled: true
  broker_host: 118.196.46.169
  broker_port: 1883
  client_id: iotgw-dev
  username: "zg"
  password: "12345678"
  keepalive_sec: 30
  clean_session: true
  topic_prefix: "iotgw/dev/"
