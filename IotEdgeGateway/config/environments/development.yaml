name: IotEdgeGateway
env: development

paths:
  config_root: IotEdgeGateway/config
  data_dir: data
  log_file: data/logs/iotgw-dev.log
  www_root: /Users/ciyeer/iwork/Iot-gateway/IotEdgeGateway/www  # Absolute path for local dev

logging:
  level: info
  file_sink_enabled: true

network:
  http_api:
    host: 0.0.0.0
    port: 8088
    base_path: /api
  websocket:
    path: /ws

mqtt:
  enabled: true
  broker_host: 118.196.46.169
  broker_port: 1883
  client_id: iotgw-dev
  username: "zg"
  password: "12345678"
  keepalive_sec: 30
  clean_session: true
  topic_prefix: "iotgw/dev/"
