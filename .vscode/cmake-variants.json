{
  "buildType": {
    "default": "debug",
    "choices": {
      "debug": {
        "short": "Debug",
        "long": "Debug build",
        "buildType": "Debug"
      },
      "release": {
        "short": "Release",
        "long": "Release build",
        "buildType": "Release"
      }
    }
  },
  "arch": {
    "default": "x86_64",
    "choices": {
      "x86_64": {
        "short": "x86_64",
        "long": "x86_64"
      },
      "aarch64": {
        "short": "aarch64",
        "long": "aarch64"
      }
    }
  }
}
